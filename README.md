pwd — Displays the present working directory.
ls — Lists files and directories in the current directory.
ls -la — Lists all files, including hidden files, with detailed information.
cd — Changes the current working directory.
mkdir — Creates a new directory.
touch — Creates a new empty file or updates a file's timestamp.
cat — Displays the contents of a file.
cp — Copies files or directories.
mv — Moves or renames files and directories.
rm — Removes files or directories.
df -h — Displays disk space usage of mounted filesystems in human-readable format.
du -sh — Displays the total size of a file or directory in human-readable format.
free -h — Displays RAM and swap memory usage in human-readable format.
top — Displays running processes and system resource usage in real time.
ps aux — Displays detailed information about all running processes.
kill — Terminates a process using its process ID (PID).
whoami — Displays the username of the currently logged-in user.
ip addr — Displays network interface and IP address information.
ping — Tests network connectivity to another host.
systemctl — Manages system services and systemd processes.
clear — Clears the contents of the terminal screen.
date — Displays the current date and time.
cal — Displays a calendar in the terminal.
hostname — Displays the system's host name.
uname — Displays information about the operating system and kernel.
history — Displays previously executed commands.
echo — Displays text or variable values in the terminal.
man — Displays the manual/help page for a Linux command.
head — Displays the first lines of a file.
tail — Displays the last lines of a file.
less — Displays file contents page by page for easy viewing.
grep — Searches for matching text or patterns in files or command output.
find — Searches for files and directories based on specified conditions.
which — Shows the location/path of an executable command.
wc — Counts lines, words, and characters/bytes in files.
sort — Sorts lines of text alphabetically or numerically.
chmod — Changes the permissions of files and directories.
chown — Changes the owner and/or group of files and directories.
sudo — Executes a command with elevated privileges, usually as another user.
apt — Manages software packages on Debian-based Linux systems.
ip addr — Displays all network interfaces and their IP address information.
eth0 — Common name for an Ethernet network interface.
link/ether — Displays the MAC (hardware) address of the network interface.
inet — Displays the IPv4 address assigned to an interface.
inet6 — Displays the IPv6 address assigned to an interface.
mtu — Displays or configures the Maximum Transmission Unit size of a network interface.
hostname -i — Displays the IP address associated with the system hostname.
ip route — Displays the system's IP routing table.
ping — Tests network connectivity between the system and another host.
time — Measures how long a command takes to execute.
ss -tuln — Displays listening TCP/UDP network sockets with numeric addresses and ports.
ss -t — Displays TCP sockets.
ss -u — Displays UDP sockets.
ss -l — Displays listening sockets.
ss -n — Displays numeric addresses and port numbers without resolving names.
TCP — A connection-oriented protocol used for reliable data transmission.
UDP — A connectionless protocol used for faster data transmission without guaranteed delivery.
HTTP — A protocol used for transferring web pages and web data.
HTTPS — A secure, encrypted version of HTTP.
traceroute — Shows the path or network hops packets take to reach a destination.
ps — Displays information about currently running processes.
pid — Process ID, the unique number assigned to a running process.
tty — Displays the terminal device associated with the current shell.
cmd — Refers to the command being executed by a process.
ps aux — Displays detailed information about all running processes and their users.
top — Provides a real-time view of running processes and system resource usage.
pstree — Displays running processes in a tree structure showing parent-child relationships.
kill — Sends a signal to a process, commonly to terminate it.
nslookup — Queries DNS to find information such as the IP address of a domain.
curl — Transfers data to or from a server using protocols such as HTTP and HTTPS.
ps aux — Lists all running processes with detailed information.
kill PID — Terminates or sends a signal to the process specified by its PID.
systemctl status — Displays the current status of a systemd service.
